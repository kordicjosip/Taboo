import {NgxLoggerLevel} from "ngx-logger";

export const environment = {
  production: true,
  apiURL: 'https://dalekaobala.ba/api/',
  wsURL: 'wss://dalekaobala.ba/api/ws/',
  logLevel: NgxLoggerLevel.OFF
};
